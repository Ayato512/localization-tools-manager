# 依赖清单
# 跨平台
PyQt6>=6.4.0
pandas>=2.0.0
openpyxl>=3.1.0
Pillow>=10.0.0
openai>=1.0.0
rapidfuzz>=3.0.0
jieba>=0.42.1
lxml>=4.9.0

# 仅 Windows 需要（Mac 上会自动跳过；"Excel 搜索器"工具用到）
pywin32>=306; sys_platform == "win32"
